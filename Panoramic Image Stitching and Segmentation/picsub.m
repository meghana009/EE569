function y = picsub(x,m,n)
    
    y(1,1,:) = x(m - 2, n - 2, :);
    y(1,2,:) = x(m - 1, n - 2, :);
    y(1,3,:) = x(m , n - 2, :);
    y(1,4,:) = x(m + 1, n - 2, :);
    y(1,5,:) = x(m + 2, n - 2, :);

    y(2,1,:) = x(m - 2, n - 1, :);
    y(2,2,:) = x(m - 1, n - 1, :);
    y(2,3,:) = x(m  , n - 1, :);
    y(2,4,:) = x(m + 1, n - 1, :);
    y(2,5,:) = x(m + 2, n - 1, :);

    y(3,1,:) = x(m - 2, n , :);
    y(3,2,:) = x(m - 1, n , :);
    y(3,3,:) = x(m , n , :);
    y(3,4,:) = x(m + 1, n , :);
    y(3,5,:) = x(m + 2, n , :);

    y(4,1,:) = x(m - 2, n + 1, :);
    y(4,2,:) = x(m - 1, n + 1, :);
    y(4,3,:) = x(m , n + 1, :);
    y(4,4,:) = x(m + 1, n + 1, :);
    y(4,5,:) = x(m + 2, n + 1, :);

    y(5,1,:) = x(m - 2, n + 2, :);
    y(5,2,:) = x(m + 1, n + 2, :);
    y(5,3,:) = x(m , n + 2, :);
    y(5,4,:) = x(m + 1, n + 2, :);
    y(5,5,:) = x(m + 2, n + 2, :);

end

    